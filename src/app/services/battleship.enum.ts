export enum EGameState {
    WON = 'won',
    PLAYING = 'play',
    LOST = 'lost',
} 

export enum EGameLevel {
    CUSTOM = 0,
    EASY = 1,
    MEDIUM = 2,
    HARD = 3,
}

export enum EBoardWidth {
    THIN = 'thin',
    NORMAL = 'normal',
    WIDE = 'wide',
}
